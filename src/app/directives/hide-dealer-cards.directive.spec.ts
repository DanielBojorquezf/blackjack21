import { HideDealerCardsDirective } from './hide-dealer-cards.directive';

describe('HideDealerCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideDealerCardsDirective();
    expect(directive).toBeTruthy();
  });
});
